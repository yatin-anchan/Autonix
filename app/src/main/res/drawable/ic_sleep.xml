<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/marker_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="6dp">

    <!-- Pin background (circle + tail) -->
    <ImageView
        android:id="@+id/marker_bg"
        android:layout_width="56dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:src="@drawable/marker_bg"
        android:contentDescription="marker background" />

    <!-- Sleep icon: emoji for simplicity (keeps everything xml-only) -->
    <TextView
        android:id="@+id/marker_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="8dp"
        android:text="😴"
        android:textSize="28sp"
        android:includeFontPadding="false"
        android:gravity="center" />

</FrameLayout>
